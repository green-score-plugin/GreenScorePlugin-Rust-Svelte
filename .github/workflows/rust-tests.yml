name: Tests Backend Rust

on:
  push:
  pull_request:
    branches: ["main", "master"]
  workflow_dispatch:

permissions:
  checks: write
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            backend/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('backend/Cargo.lock') }}

      - name: Cargo check
        run: cargo check --verbose

      - name: Run tests
        if: always()
        run: cargo test --no-fail-fast -- -Z unstable-options --format json --report-time | tee results.json
        continue-on-error: true

      - name: Convert to JUnit
        if: always()
        run: |
          cargo install cargo2junit
          cat results.json | cargo2junit > junit-report.xml

      - name: Publish test report
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Résultat Tests Backend
          path: backend/junit-report.xml
          reporter: java-junit
          fail-on-empty: false