name: Tests Backend Rust

on:
  push:
  pull_request:
    branches: ["main", "master"]
  workflow_dispatch:

permissions:
  checks: write
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Récupération du code
        uses: actions/checkout@v4

      - name: Installation de Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Installation de cargo-nextest
        uses: taiki-e/install-action@nextest

      - name: Cache des dépendances Cargo
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            backend/target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('backend/Cargo.lock') }}

      - name: Vérification de la compilation
        run: cargo check --verbose

      - name: Lancement des tests
        run: cargo nextest run --junit-path junit-report.xml

      - name: Debug - Vérification fichiers backend
        if: always()
        run: ls -la

      - name: Publication du rapport de tests
        uses: dorny/test-reporter@v1
        if: success() || failure()
        with:
          name: Résultat Tests Backend
          path: backend/junit-report.xml
          reporter: java-junit
